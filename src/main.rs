fn main() {
    //Arrays are declarable through [] brackets. 
    let a = [1, 2, 3];
    let mut index = 0;
    println!("array position {} = {}", index+1, a[index]);
    index+=1;
    println!("array position {} = {}", index+1, a[index]);
    index+=1;
    println!("array position {} = {}", index+1, a[index]);

}
